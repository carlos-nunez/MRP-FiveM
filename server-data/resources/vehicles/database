ALTER TABLE `owned_vehicles` ADD `X` DECIMAL(10,2) NULL DEFAULT NULL AFTER `plate`, ADD `Y` DECIMAL(10,2) NULL DEFAULT NULL AFTER `X`, ADD `Z` DECIMAL(10,2) NULL DEFAULT NULL AFTER `Y`, ADD `heading` DECIMAL(10,2) NULL DEFAULT NULL AFTER `Z`;
